# Sprint 1.1 Popup DOM Hotfix

This hotfix restores the Sprint 1.1 popup DOM elements required by `popup.ts` and `popup.js`:

- `#recentList`
- `#recentEmpty`
- `#openLog`

Apply it to the repository root, then run `npm.cmd run check` and reload the unpacked extension in Chrome.
