# DSSI Core A v0.4.4 更新手順

Sprint 3.3の表示系を、通信パルス中心の観測へ調整する更新です。

## 主な変更

- 通信文章チップを初期OFFにし、小型通信パルスを標準表示にした
- 文章チップと通信パルスを別レーンへ配置した
- 10秒、30秒、60秒、手動消去まで固定の保持時間を追加した
- ページ上で一時停止、消去、パルス非表示、文章チップ非表示を切り替えられる
- 一時切替はページ再読み込みで保存済み基本設定へ戻る
- 観測ログの初期表示を、通常・診断を統合した簡易ストリームにした
- 各行を開くと詳細を確認でき、従来の全列表にも切り替えられる

## 適用

```powershell
cd "C:\UserWorkspace\10_PUBLIC_GITHUB\DSSI\DSSI-Core-A"

Expand-Archive `
  "$HOME\Downloads\DSSI-Core-A-v0.4.4-update.zip" `
  -DestinationPath "." `
  -Force
```

## 検査とビルド

```powershell
npm.cmd run format
npm.cmd run check
```

正常時のpackage versionは`0.4.4`です。

## Chrome反映

1. `chrome://extensions`でDSSI Core Aを再読み込みする。
2. 観測対象ページも再読み込みする。
3. `dist/manifest.json`が`0.4.4`であることを確認する。

## 手動確認

- 通信文章チップが初期状態では出ず、通信パルスだけが出る
- パルスが文章チップに隠れない
- 10秒、30秒、60秒、固定保持を選べる
- パルス周辺の操作部で一時停止、消去、表示抑制、文章チップ抑制を行える
- ページ再読み込みで一時設定が基本設定へ戻る
- 観測ログが全時系列の簡易ストリームで開く
- 通常のみ、診断のみ、詳細表へ切り替えられる
- 簡易行を開くと従来の詳細情報を確認できる
- 本文、Cookie値、URL path/queryが表示されない
