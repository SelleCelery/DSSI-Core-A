# DSSI Core A Sprint 3.4 更新手順

## 対象

- 更新後バージョン: `0.4.6`
- Sprint: `3.4`
- 主題: ドメイン別観測表示とログ保存境界
- 観測記録スキーマ: `10`

Sprint 3.4は、通信表示の密度をhostname単位で調整・保存できるようにし、DSSIの一次観測記録をJSONまたはCSVへ書き出します。ホスト別設定は安全性・信頼性・許可の評価ではなく、表示方法だけを保存する観測表示プロファイルです。

## 更新前

現在の作業を確認します。

```powershell
cd "C:\UserWorkspace\10_PUBLIC_GITHUB\DSSI\DSSI-Core-A"
git status
```

動作確認済みのSprint 3.3.2までを先にコミットしてから更新することを推奨します。

## 更新ZIPの展開

```powershell
Expand-Archive `
  "$HOME\Downloads\DSSI-Core-A-Sprint3.4-update.zip" `
  -DestinationPath "." `
  -Force
```

## 検査とビルド

```powershell
npm.cmd run format
npm.cmd run check
```

`npm run check`は、型検査、ESLint、Prettier確認、単体テスト、ビルドを順に実行します。

## Chromeへの反映

1. `chrome://extensions`を開く。
2. DSSI Core Aの「再読み込み」を押す。
3. 観測対象ページも再読み込みする。
4. 必要に応じてオプション画面で通信観測権限を確認する。

## 主な確認項目

### ホスト別表示プロファイル

Level 2、Level 3、またはMAXで通信パルスHUDを表示し、次を確認します。

- `◉ / ○`: 現在のhostnameで通信パルスを表示・非表示
- `T`: 現在のhostnameで通信説明チップを表示・非表示
- `D`: DOM観測色を循環
- `W`: webRequest観測色を循環
- `α`: 通信パルス不透明度を循環
- `↺`: hostname別表示プロファイルを削除し、全体設定へ戻す

色の循環は次です。

```text
マゼンタ → シアン → イエロー → 無色 → マゼンタ
```

不透明度は次です。

```text
100% → 80% → 60% → 40% → 100%
```

位置変更もhostname単位で保存されます。通信表示を非表示にしても、粘性レベルに基づくパスワード・決済情報・個人情報等の注意チップは非表示になりません。

### 初回観測と再確認

初めて観測するhostnameでは「初回観測ホスト」を表示します。DSSIは粘性レベルを自動変更しません。

hostname別表示プロファイルが90日以上更新されていない場合、「ホスト表示設定の再確認」を表示できます。これは安全性の変化を判定した通知ではなく、過去の表示判断を見直す契機です。

### ログ保存

観測ログ画面の「観測ログを保存」から確認します。

- JSON
- CSV＋context JSON
- JSON＋CSV
- 全ログ
- 現在表示中

JSONには次を含みます。

- DSSIバージョン
- 観測記録スキーマ
- 保存日時と保存範囲
- エクスポート時設定
- 観測時設定スナップショット（取得できた記録）
- Coverage Manifest
- 非保存対象
- 利用境界
- 一次観測記録
- `integrity.status: not_provided`

CSVは一行一観測記録です。CSVだけを選ぶと、設定・観測範囲・利用境界を保持するcontext JSONも同時に保存します。

保存時に通信の集約、危険度判定、通信目的分類、欠損値補完は行いません。ただし、全時系列表示へ統合するため、選択と時刻降順の整列は行います。観測記録の各フィールド値は変換しません。

## 現在の保存境界

保存後のファイルはブラウザのダウンロード先へ移ります。v0.4.6では、次を提供しません。

- 暗号化
- デジタル署名
- 改変検知
- 書き込み禁止
- DSSI生成物であることの真正性証明

ログは、利用者自身の照合と判断支援を主目的とする一次観測記録です。利用者の意図、責任、通信内容、有害性、安全性を証明しません。
